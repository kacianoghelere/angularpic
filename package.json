{
  "name": "angularpic",
  "description": "A really cool app",
  "version": "1.0.0",
  "engines": {
    "node": "4.6.2"
  },
  "scripts": {
    "install": "cd server && npm install",
    "postinstall": "../client && npm install",
    "start": "cd ../server && npm start"
  }
}