{
  "name": "angularpic",
  "description": "A really cool app",
  "version": "1.0.0",
  "engines": {
    "node": "4.6.2",
    "npm": "2.15.11"
  },
  "scripts": {
    // "heroku-prebuild": "cd server && npm install && cd ../client && npm install",
    "heroku-postbuild": "cd server && npm start"
  }
}